# ГРАБЛІ

- 2026-09-15 — `chromium.launch()` з Playwright із `@playwright/mcp` падає: немає `chromium_headless_shell-1212` (браузери не завантажені після оновлення). Обхід без `npx playwright install` — `chromium.launch({ channel: 'chrome' })`, бере встановлений Google Chrome.
- 2026-09-15 — Upwork, форма Employment: `fill()` і `keyboard.insertText()` у typeahead-полях (Company/Country/Title) дописують до старого значення й не відкривають список підказок. Працює: кнопка `.air3-btn-clear` поруч із полем → `click({force:true})` → `pressSequentially()`. Перші символи інколи губляться (замість «PHP / WordPress…» записалось « WordPress…») — звіряти `inputValue()` і повторювати.
- 2026-09-15 — Upwork: `Escape` у полі модалки закриває весь діалог без збереження. Country треба саме обрати зі списку (`[role=option]` у `#aria-controls`, події mousedown/mouseup/click), інакше «Field is required».
- 2026-09-15 — Upwork Employment: встановлення `value` через нативний setter + `input` показує текст у полі, але Vue-модель лишається порожньою («Field is required»). Надійного автоматичного вводу в typeahead Title/Company не знайшлося — ці поля простіше правити руками.
