# ЗНАХІДКИ

- 2026-09-15 — Метрики Upwork (JSS, Top Rated, total jobs/hours/earnings) видно на власному профілі `upwork.com/freelancers/~013563a358160baa29` у залогіненому Chrome. Старі URL списку контрактів (`/nx/wm/contracts`, `/ab/contracts/`) дають 404, а `/freelancers/contracts` веде на чужий профіль — лічильник контрактів звідти не взяти.
- 2026-09-15 — Редагування overview на Upwork: кнопка `button[aria-label="Edit description"]`; коли вікно Chrome не на передньому плані, Playwright-клік висне — працює `element.click()` через `evaluate`, а `textarea.fill()` і Save теж через `evaluate`.
